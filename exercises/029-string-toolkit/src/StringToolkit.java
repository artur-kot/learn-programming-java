
public class StringToolkit {

    // Part 1: Character and Word Counting

    /**
     * Count the number of characters in a string
     * Example: countCharacters("Hello") returns 5
     */
    public int countCharacters(String str) {
        // TODO: Return the length of the string
        return 0;
    }

    /**
     * Count the number of words (strings) in an array
     * Example: countWords({"cat", "dog", "bird"}) returns 3
     */
    public int countWords(String[] words) {
        // TODO: Return the number of elements in the array
        return 0;
    }

    // Part 2: Finding Longest and Shortest

    /**
     * Find the longest string in an array
     * If multiple strings have the same longest length, return the first one
     * Example: findLongest({"cat", "elephant", "dog"}) returns "elephant"
     */
    public String findLongest(String[] words) {
        // TODO: Loop through the array and track the longest string
        // Hint: Start with the first element as the longest, then compare each element
        return "";
    }

    /**
     * Find the shortest string in an array
     * If multiple strings have the same shortest length, return the first one
     * Example: findShortest({"cat", "elephant", "dog"}) returns "cat"
     */
    public String findShortest(String[] words) {
        // TODO: Loop through the array and track the shortest string
        // Hint: Start with the first element as the shortest, then compare each element
        return "";
    }

    // Part 3: String Joining

    /**
     * Join all strings together with a separator between them
     * Example: concatenateWords({"Java", "is", "fun"}, " ") returns "Java is fun"
     * Example: concatenateWords({"red", "green", "blue"}, ", ") returns "red, green, blue"
     */
    public String concatenateWords(String[] words, String separator) {
        // TODO: Build a result string by joining all words with the separator
        // Hint: Add the first word, then loop and add separator + word for the rest
        // Hint: Or build the string with separators and remove the last separator
        return "";
    }
}
