
public class ArrayBasics {

    // Part 1: Declare and Print Arrays
    /**
     * Print each array element on a separate line
     */
    public void printArray(int[] numbers) {
        // TODO: Iterate through array and print each element on a new line
    }

    /**
     * Print all array elements in one line, separated by ", "
     */
    public void printArrayInline(int[] numbers) {
        // TODO: Print all elements in format: "1, 2, 3"
    }

    /**
     * Print each array element with its index Format: "Index 0: 1", "Index 1:
     * 2", etc.
     */
    public void printArrayWithIndex(int[] numbers) {
        // TODO: Print each element with its index position
    }

    // Part 2: Array Aggregation
    /**
     * Calculate sum of all array elements Example: {1, 2, 3} returns 6
     */
    public int calculateSum(int[] numbers) {
        // TODO: Add all elements and return the sum
        return 0;
    }

    /**
     * Calculate average of all array elements Example: {1, 2, 3} returns 2.0
     */
    public double calculateAverage(int[] numbers) {
        // TODO: Calculate and return average as double
        return 0;
    }

    /**
     * Find the minimum value in the array Example: {5, 2, 8} returns 2
     */
    public int findMin(int[] numbers) {
        // TODO: Compare all elements and return the smallest
        return 0;
    }

    /**
     * Find the maximum value in the array Example: {5, 2, 8} returns 8
     */
    public int findMax(int[] numbers) {
        // TODO: Compare all elements and return the largest
        return 0;
    }

    // Part 3: Array Count
    /**
     * Count how many even numbers are in the array Example: {1, 2, 3, 4}
     * returns 2
     */
    public int countEven(int[] numbers) {
        // TODO: Count elements divisible by 2
        return 0;
    }

    /**
     * Count how many odd numbers are in the array Example: {1, 2, 3, 4} returns
     * 2
     */
    public int countOdd(int[] numbers) {
        // TODO: Count elements not divisible by 2
        return 0;
    }

    /**
     * Count how many array elements are greater than threshold Example: {1, 5,
     * 3, 8} with threshold 4 returns 2
     */
    public int countGreaterThan(int[] numbers, int threshold) {
        // TODO: Count elements greater than the threshold
        return 0;
    }
}
