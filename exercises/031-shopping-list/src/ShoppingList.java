import java.util.ArrayList;

public class ShoppingList {

    /**
     * Add an item to the shopping list
     * Example: addItem(list, "Milk") adds "Milk" to the end of the list
     */
    public void addItem(ArrayList<String> list, String item) {
        // TODO: Add the item to the list
    }

    /**
     * Remove an item from the shopping list
     * Example: removeItem(list, "Milk") removes the first occurrence of "Milk"
     * If the item doesn't exist, the list remains unchanged
     */
    public void removeItem(ArrayList<String> list, String item) {
        // TODO: Remove the item from the list
    }

    /**
     * Search for an item in the shopping list
     * Returns the index position where the item is found, or -1 if not found
     * Example: In ["Bread", "Milk", "Eggs"], searching "Milk" returns 1
     */
    public int searchItem(ArrayList<String> list, String item) {
        // TODO: Return the index of the item, or -1 if not found
        return -1;
    }

    /**
     * Count how many times an item appears in the shopping list
     * Example: In ["Milk", "Bread", "Milk"], counting "Milk" returns 2
     */
    public int countOccurrences(ArrayList<String> list, String item) {
        // TODO: Loop through list and count matching items
        return 0;
    }

    /**
     * Display all items in the shopping list
     * Print each item on a separate line with a number
     * If list is empty, print "Shopping list is empty"
     * Example output:
     * 1. Bread
     * 2. Milk
     * 3. Eggs
     */
    public void displayList(ArrayList<String> list) {
        // TODO: Print all items, or "Shopping list is empty" if empty
    }
}
